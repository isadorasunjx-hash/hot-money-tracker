---
name: hot-money-tracker
description: >
  Solana链上量能突破策略，自动监控代币1小时交易量是否突破24小时均值3倍，结合持仓分析和链上买单比例验证动量，在风控范围内执行买入并全自动止损止盈。
  触发词：启动热钱追踪 / 开始量能监控 / 运行热钱策略 / hot money tracker
---

# 热钱追踪者 (Hot Money Tracker)

> 策略类型：Solana 链上量能突破 + 聪明钱动向
> 作者：@isadora288881
> 版本：v1.0
> OnchainOS 工具依赖：okx-dex-token / okx-dex-market / okx-wallet-portfolio / okx-dex-swap / okx-onchain-gateway

## 策略概述

监控 Solana 链上短期量能异常放大的代币。当1小时交易量超过24小时均值3倍时触发初步信号，结合链上持仓分布和买单比例二次验证，最终在风控许可范围内完成建仓，并持续监控执行自动止损/止盈。

核心逻辑：**量在价先**——真实的启动行情必然伴随交易量异常放大，在量能确认后入场，不追高、不赌方向。

## 执行流程

### 阶段一：信号扫描（每5分钟运行一次）

调用 `okx-dex-token`，获取 Solana 链上过去1小时交易量排名前50的代币列表。

筛选条件（全部满足才继续）：

- 1h交易量 ≥ 24h均值每小时交易量 × 3
- 代币市值 ≥ $500,000
- 代币上线时间 ≥ 24小时
- 持仓地址数 ≥ 200
- 前10地址持仓合计占比 ≤ 60%

### 阶段二：动量验证

对通过阶段一的代币，调用 `okx-dex-market` 获取最近15分钟K线和链上交易明细。

验证条件：

- 当前价格 ≥ 过去1小时最低价 × 1.10
- 当前价格 ≤ 过去1小时最高价 × 0.95
- 最近20笔链上交易中，买单笔数占比 ≥ 60%

### 阶段三：风控检查

调用 `okx-wallet-portfolio` 获取当前钱包状态。

- 当前USDC可用余额 ≥ $20
- 现有持仓代币数量 < 3
- 本次入场金额 = min(总资产 × 5%, $200)

### 阶段四：买入执行

调用 `okx-dex-swap` 构建交易参数 USDC → 目标代币，滑点2%。

调用 `okx-onchain-gateway` 先模拟交易，确认无异常后广播。

### 阶段五：持仓监控（每5分钟检查一次）

止损触发：当前价格 ≤ 入场价 × 0.85 或 1h量较入场时下降 ≥ 70%

止盈触发：当前价格 ≥ 入场价 × 1.40 或 价格 ≥ 入场价 × 1.20 且高点回调 ≥ 10%

卖出：调用 `okx-dex-swap` 将代币换回 USDC，滑点3%。

## 风控参数

| 参数 | 数值 |
|------|------|
| 单笔仓位上限 | 总资产 5% 或 $200（取小值） |
| 最大并发持仓 | 3 个 |
| 硬止损 | -15% |
| 固定止盈 | +40% |
| 动态止盈 | +20% 且回调 10% |
| 入场滑点 | 2% |
| 出场滑点 | 3% |
| 量能突破倍数 | ≥ 3x |
| 监控频率 | 每 5 分钟 |

## 快速启动

用户发送以下任意一句即可激活：

- 启动热钱追踪策略
- 开始量能监控
- 运行 hot money tracker
